# Flyway DEV Environment Configuration
# Extends flyway-base.conf — only overrides DEV-specific settings.

# Allow out-of-order migrations for developer convenience
flyway.outOfOrder=true

# Automatically baseline fresh dev databases
flyway.baselineOnMigrate=true

# Show query results in info output (helps debugging)
flyway.outputQueryResults=true
